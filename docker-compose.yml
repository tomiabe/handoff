services:
  ws-server:
    build: ./server
    ports:
      - "8080:8080"

  web:
    build:
      context: .
      args:
        NEXT_PUBLIC_WS_URL: ws://localhost:8080
    ports:
      - "3000:80"
    depends_on:
      - ws-server
